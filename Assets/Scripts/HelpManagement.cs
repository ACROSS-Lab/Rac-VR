using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelpManagement : MonoBehaviour
{
    public GameObject PanelTuto;
    public Vector3 start_pos = new Vector3(0.189f, -0.858f, -0.718f);
    public Quaternion start_rot;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
