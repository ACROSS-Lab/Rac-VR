using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelpManagement : MonoBehaviour
{
    public GameObject PanelTuto;
    public GameObject Complete_XR;
    // public GameObject XR;
    public Vector3 start_pos = new Vector3(0f, 0f, 0f);
    public Quaternion start_rot = new Quaternion(0f, 0f, 0f, 0f);
    // public Vector3 start_pos_xr = new Vector3(0f, 0f, 0f);
    // public Quaternion start_rot_xr = new Quaternion(0f, 0f, 0f, 0f);

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
